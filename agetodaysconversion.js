// Pseudo Code to Convert Age to Days

function calcAge (age) {
	return age * 365;
}
	console.log(calcAge(65));

// Function which takes an input number as an age and outputs the amount of days in total from that age.
